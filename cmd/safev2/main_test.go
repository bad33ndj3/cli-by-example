package main

import (
	"testing"
)

func TestCommand_Run(t *testing.T) {
	//safe := &mocks.Safe{}
	//safe.On("SetName", mock.Anything)
	//safe.On("GetSecretName").Return("secretName")
	//safe.On("GetName").Return("name")

	// todo: add test before the big presentation!!!
}
